#ifndef MODEL_DATA_H
#define MODEL_DATA_H

#include <stdint.h>

/*
 * Placeholder model data.
 * Later, the Python compiler will generate this automatically.
 */

static const uint8_t model_data[] = {
    0x00,
    0x01,
    0x02,
    0x03
};

static const uint32_t model_data_size = sizeof(model_data);

#endif